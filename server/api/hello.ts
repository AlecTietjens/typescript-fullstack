import { hello } from '../../db/models';

export const router = require('express').Router();

router.get('/', async (req, res) => {
//   try {
//   } catch (err) {
//     res.status(500).send(err);
//   }
});
